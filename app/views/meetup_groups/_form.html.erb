<%= form_for(@meetup_group) do |f| %>
  <% if @meetup_group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@meetup_group.errors.count, "error") %> prohibited this meetup_group from being saved:</h2>

      <ul>
      <% @meetup_group.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
    <%  if current_user?(@meetup_group.owner) %>

        <%= link_to 'Add Activity', new_activity_path(activity: {meetup_group_id: @meetup_group.id}),
                    class: "btn btn-large btn-primary"   %>
        <%= link_to 'Add Member', new_membership_path(membership: {meetup_group_id: @meetup_group.id}),class: "btn btn-large btn-primary"  %>
    <% else %>

    <% end %>
    <%= f.hidden_field :owner_id %>
  <div class="field">
    <%= f.label :topic %><br>
    <%= f.text_field :topic %>
  </div>
  <div class="field">
    <%= f.label :incepted_at %><br>
    <%= f.date_select :incepted_at %>
  </div>
  <div class="field">
    <%= f.label :home_town %><br>
    <%= f.text_field :home_town %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
